Sort s1 => dac.left;
// Sort s2 => dac.right;

s1.setBubble();
// s2.setSelection();

/*
for (int i; i < 15; i++) {
    for (int j; j < 5; j++) {
        chout <= s.last() <= "-" <= s.position() <= "|";
        1::samp => now;
    }
    chout.flush();
    <<< " - Iteration: ", s.iteration(), " - Size:", s.size() >>>;
}

*/
s1.freq(50);
// s2.freq(50);
day => now;
